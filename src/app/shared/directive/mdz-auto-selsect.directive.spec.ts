import { MdzAutoSelsectDirective } from './mdz-auto-selsect.directive';

describe('MdzAutoSelsectDirective', () => {
  it('should create an instance', () => {
    const directive = new MdzAutoSelsectDirective();
    expect(directive).toBeTruthy();
  });
});
